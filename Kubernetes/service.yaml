apiVersion: v1
kind: Service
metadata:
  name: simple-flask-app:latest
  labels:
    app: simple-flask-app:latest
spec:
  type: LoadBalancer
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: simple-flask-app:latest
